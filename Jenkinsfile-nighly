pipeline {
    agent any

    stages {

        stage('Build Dockerfile image'){
            steps {
                sh '''
                    docker build -t my-playwright .
                '''
            }
        }
}
